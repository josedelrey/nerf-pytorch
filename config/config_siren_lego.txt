# =============================================================================
# SIREN Configuration File for Lego Dataset
# =============================================================================

# --------------------
# Log Parameters
# --------------------
log_root = ./logs                  # Name of the root logs folder
experiment_name = siren            # Name of this experiment's log subfolder

# --------------------
# Dataset Parameters
# --------------------
dataset_path = ./dataset/lego     # Directory containing the dataset

# --------------------
# Sampling Parameters
# --------------------
num_random_rays = 1024             # Number of random rays sampled per iteration
chunk_size = 1024                  # Number of rays processed in a single rendering batch
num_samples = 128                  # Number of samples along each ray

# --------------------
# Training Parameters
# --------------------
num_iters = 300000                 # Total number of training iterations
learning_rate = 1e-4               # Starting learning rate for the optimizer
near = 2.0                         # Near bound for ray sampling
far = 6.0                          # Far bound for ray sampling

# --------------------
# Model Saving Parameters
# --------------------
save_root = ./models               # Directory to save trained models
save_interval = 5000               # Interval (in iterations) for saving model checkpoints

# --------------------
# Learning Rate Decay Parameters
# --------------------
lr_decay = 300                     # Number of 1000-step intervals for decay
lr_decay_factor = 0.1              # Factor to multiply the learning rate at decay
lr_min = 1e-5                      # Minimum learning rate possible

# --------------------
# Monitoring Parameters
# --------------------
log_interval = 50                  # Interval (in iterations) for logging training metrics
val_interval = 10000               # Interval (in iterations) for running validation

# --------------------
# Monitoring Parameters
# --------------------
first_step_render = false          # First step validation render flag

# --------------------
# Evaluation Render Parameters
# --------------------
num_render_poses = 80              # Number of frames of the evaluation video
num_samples_eval = 256             # Number of samples per ray for evaluation

# --------------------
# Model Type
# --------------------
model_type = siren

# --------------------
# Model Hyperparameters
# --------------------
num_layers = 8
siren_hidden_dim = 256
siren_dir_encoding_dim = 4
sigma_mul = 10.0
rgb_mul = 1.0
w0 = 30.0
hidden_w0 = 1.0

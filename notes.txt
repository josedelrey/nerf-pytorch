TODO:
- Run multiscalewavelet_2 experiment to see how much the slower hyperparams are needed
- Remove sigma_mul and rgb_mul
- Try alpha = beta = 1
- Check if the double frequency wavelet filter is actually making a difference by putting the same frequency to both
- Try to set independent wavelet filters and not shared
- Try to apply the normalization applied in the implicit-neural-representations repo version
- Check weight initialization
- See if low_omega0 and high_omega0 are being properly learnt or if gradient is too small
- Detect bottlenecks
- Improve bottlenecks
- Run model hyperparameter space search (specially for choosing low_omega0, high_omega0, alpha and beta)
- Implement GaborNet and FourierNet NeRF models to compare to.
